# flask-Static-Dynamic-Routes
What are Static Routes?  A static route is a fixed URL that always returns the same response. The URL does not change and does not accept any user input. What are Dynamic Routes?  A dynamic route accepts values from the URL. Flask stores these values in variables and passes them to a function.
